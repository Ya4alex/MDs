# 2. Функции
### 1. Модернизация системы вывода
Разработайте функцию `modern_print`, которая принимает строку и выводит её, если она не была выведена ранее.

**Пример 1:**

```c
<
modern_print("Hello!") modern_print("Hello!") modern_print("How do you do?") modern_print("Hello!")

>
Hello! 
How do you do?
```

**Пример 2:**

```c
<
modern_print("Ало!") modern_print("Ало!") modern_print("Я тебя не слышу") modern_print("Ало!") modern_print("Ало!") modern_print("Позвони когда сможешь") modern_print("Позвони когда сможешь") modern_print("Я тебя не слышу")

>
Ало! 
Я тебя не слышу 
Позвони когда сможешь
```

### 2. Генератор списков
Большинство задач этой главы ориентировано на отработку навыков по разработке функций.

Ваше решение будет использоваться как библиотека.

Напишите функцию `make_list`, которая создаёт, заполняет и возвращает список заданного размера.

Параметры функции:

- `length` — требуемая длина списка;
- `value` — значение элементов списка (по-умолчанию `0`).


### 3. Среднее
Напишите функцию `average`, которая может принимать неограниченное количество чисел (как параметры функции) или один список (как один параметр). Функция должна вернуть среднее значение всех чисел.

### 4. Подготовка данных
Напишите функцию `to_string`, которая формирует из последовательности данных строку.  
Функция должна принимать:

- неопределённое количество данных;
- необязательный параметр `sep` (по умолчанию пробел);
- необязательный параметр `end` (по умолчанию `\n`).

**Пример:**

```python
<
data = [7, 3, 1, "hello", (1, 2, 3)] result = to_string(*data, sep=", ", end="!")

>
result = '7, 3, 1, hello, (1, 2, 3)!'
```
